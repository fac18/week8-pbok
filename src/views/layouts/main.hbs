<!DOCTYPE html>
    <html lang="en">
            {{> head}}
        <body>
            {{>header}}
            <main>
                {{{body}}}
            </main>
            {{>footer}}
        </body>
    <script src="/script/index.js"></script>
</html>